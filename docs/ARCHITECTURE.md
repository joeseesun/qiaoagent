# 🏗️ 系统架构文档

## 📐 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         用户界面层                            │
│  ┌──────────────────┐              ┌──────────────────┐     │
│  │   用户创作界面    │              │   管理后台界面    │     │
│  │  (Next.js Page)  │              │  (Next.js Page)  │     │
│  └────────┬─────────┘              └────────┬─────────┘     │
└───────────┼──────────────────────────────────┼──────────────┘
            │                                  │
            │ HTTP/JSON                        │ HTTP/JSON
            │                                  │
┌───────────┼──────────────────────────────────┼──────────────┐
│           ▼                                  ▼               │
│  ┌─────────────────┐              ┌─────────────────┐       │
│  │  /api/workflows │              │   /api/auth     │       │
│  │  /api/run_crew  │              │   /api/config   │       │
│  └────────┬────────┘              └────────┬────────┘       │
│           │                                │                │
│           │         FastAPI 服务层          │                │
└───────────┼────────────────────────────────┼────────────────┘
            │                                │
            │                                │
            ▼                                ▼
┌──────────────────────────────────────────────────────────────┐
│                      CrewAI 工作流引擎                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   Agent 1    │  │   Agent 2    │  │   Agent 3    │       │
│  │ (Researcher) │  │   (Writer)   │  │   (Editor)   │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                 │                 │                │
│         └─────────────────┼─────────────────┘                │
│                           │                                  │
│                    ┌──────▼──────┐                           │
│                    │  Task Queue │                           │
│                    └──────┬──────┘                           │
└───────────────────────────┼──────────────────────────────────┘
                            │
                            │ API Call
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                    tu-zi.com API                              │
│              (Claude Sonnet 4.5 Model)                        │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 数据流

### 用户创作流程

```
用户输入主题
    │
    ▼
选择工作流
    │
    ▼
点击生成按钮
    │
    ▼
POST /api/run_crew
    │
    ▼
加载工作流配置 (workflows.json)
    │
    ▼
创建 Agents
    │
    ▼
创建 Tasks
    │
    ▼
执行 CrewAI 工作流
    │
    ├─► Agent 1: 研究主题
    │       │
    │       ▼
    ├─► Agent 2: 撰写文章
    │       │
    │       ▼
    └─► Agent 3: 编辑润色
            │
            ▼
    解析结果 (title, article, summary)
            │
            ▼
    返回 JSON 给前端
            │
            ▼
    显示结果卡片
```

### 管理配置流程

```
访问 /admin
    │
    ▼
输入密码
    │
    ▼
POST /api/auth
    │
    ▼
验证通过，进入后台
    │
    ▼
GET /api/config
    │
    ▼
加载当前配置
    │
    ▼
编辑 Agents/Tasks
    │
    ▼
点击保存
    │
    ▼
POST /api/config
    │
    ▼
更新 workflows.json
    │
    ▼
配置立即生效
```

## 📦 模块划分

### 前端模块

```
app/
├── page.tsx                    # 用户主页
│   ├── WorkflowSelector        # 工作流选择器
│   ├── TopicInput              # 主题输入框
│   ├── GenerateButton          # 生成按钮
│   └── ResultCard              # 结果展示卡片
│
└── admin/
    ├── page.tsx                # 登录页
    │   └── LoginForm           # 登录表单
    │
    └── dashboard/
        └── page.tsx            # 配置页
            ├── WorkflowList    # 工作流列表
            ├── AgentEditor     # Agent 编辑器
            └── TaskEditor      # Task 编辑器
```

### 后端模块

```
api/
├── workflows.py                # 工作流列表 API
│   └── get_workflows()         # 返回可选工作流
│
├── run_crew.py                 # 执行工作流 API
│   └── run_crew()              # 执行 CrewAI
│
├── config.py                   # 配置管理 API
│   ├── get_config()            # 获取配置
│   └── update_config()         # 更新配置
│
└── auth.py                     # 认证 API
    └── authenticate()          # 验证密码
```

### CrewAI 模块

```
crew/
└── main.py
    ├── load_workflow_config()  # 加载配置
    ├── create_llm()            # 创建 LLM
    ├── create_agents()         # 创建 Agents
    ├── create_tasks()          # 创建 Tasks
    ├── parse_result()          # 解析结果
    └── run_workflow()          # 执行工作流
```

## 🗄️ 数据模型

### Workflow 模型

```typescript
interface Workflow {
  name: string          // 工作流名称
  id: string            // 工作流 ID
  agents: Agent[]       // Agent 列表
  tasks: Task[]         // Task 列表
}
```

### Agent 模型

```typescript
interface Agent {
  name: string          // Agent 名称
  role: string          // 角色
  goal: string          // 目标
  prompt: string        // 提示词
}
```

### Task 模型

```typescript
interface Task {
  description: string   // 任务描述
  agent: string         // 执行的 Agent 名称
}
```

### Result 模型

```typescript
interface Result {
  title: string         // 标题
  article: string       // 正文
  summary: string       // 摘要
}
```

## 🔌 API 接口

### GET /api/workflows

**功能：** 获取可用工作流列表

**响应：**
```json
{
  "workflows": [
    {
      "id": "tech_writer",
      "name": "科技写作助手"
    }
  ]
}
```

### POST /api/run_crew

**功能：** 执行工作流

**请求：**
```json
{
  "topic": "AI 改变教育",
  "workflow_id": "tech_writer"
}
```

**响应：**
```json
{
  "title": "AI 正在重新定义教育的未来",
  "article": "...",
  "summary": "..."
}
```

### GET /api/config

**功能：** 获取工作流配置

**响应：**
```json
{
  "workflows": [...]
}
```

### POST /api/config

**功能：** 更新工作流配置

**请求：**
```json
{
  "workflows": [...],
  "password": "ai_admin_2025"
}
```

**响应：**
```json
{
  "success": true,
  "message": "Configuration updated"
}
```

### POST /api/auth

**功能：** 管理员认证

**请求：**
```json
{
  "password": "ai_admin_2025"
}
```

**响应：**
```json
{
  "authorized": true
}
```

## 🔐 安全架构

```
┌─────────────────────────────────────────┐
│           安全层                         │
│  ┌────────────────────────────────┐     │
│  │  环境变量保护                   │     │
│  │  - API Key                     │     │
│  │  - Admin Password              │     │
│  └────────────────────────────────┘     │
│                                         │
│  ┌────────────────────────────────┐     │
│  │  密码验证                       │     │
│  │  - /api/auth                   │     │
│  │  - sessionStorage              │     │
│  └────────────────────────────────┘     │
│                                         │
│  ┌────────────────────────────────┐     │
│  │  HTTPS 加密                     │     │
│  │  - Vercel 自动启用              │     │
│  └────────────────────────────────┘     │
└─────────────────────────────────────────┘
```

## 🚀 部署架构

### Vercel 部署

```
GitHub Repository
    │
    │ git push
    ▼
Vercel Build System
    │
    ├─► Build Next.js (npm run build)
    │       │
    │       ▼
    │   Static Files + Server Functions
    │
    └─► Build Python APIs (@vercel/python)
            │
            ▼
        Serverless Functions
            │
            ▼
    ┌───────────────────────────┐
    │   Vercel Edge Network     │
    │  (CDN + Serverless)       │
    └───────────────────────────┘
            │
            ▼
        用户访问
```

## 📊 性能优化

### 前端优化

```
┌─────────────────────────────────┐
│  Next.js 优化                    │
│  - 自动代码分割                  │
│  - 静态资源优化                  │
│  - 图片优化                      │
│  - 字体优化                      │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  React 优化                      │
│  - 组件懒加载                    │
│  - useMemo/useCallback          │
│  - 虚拟滚动                      │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  Tailwind CSS 优化               │
│  - PurgeCSS 移除未使用样式       │
│  - 压缩 CSS                      │
└─────────────────────────────────┘
```

### 后端优化

```
┌─────────────────────────────────┐
│  FastAPI 优化                    │
│  - 异步处理                      │
│  - 响应缓存                      │
│  - 数据验证                      │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  CrewAI 优化                     │
│  - 减少 Agent 数量               │
│  - 优化提示词                    │
│  - 并行执行任务                  │
└─────────────────────────────────┘
```

## 🔄 扩展性设计

### 水平扩展

```
添加新工作流
    │
    ▼
编辑 workflows.json
    │
    ▼
添加 Agents 和 Tasks
    │
    ▼
保存配置
    │
    ▼
立即生效（无需重启）
```

### 垂直扩展

```
升级 Vercel 计划
    │
    ├─► 更长执行时间（60s）
    ├─► 更多并发请求
    ├─► 更大内存限制
    └─► 更快 CDN
```

## 📝 总结

本系统采用现代化的前后端分离架构，通过 CrewAI 实现多 Agent 协作，支持动态配置和一键部署。整体架构清晰、模块化程度高、易于扩展和维护。

